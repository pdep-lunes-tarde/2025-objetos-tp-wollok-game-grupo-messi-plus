import tpIntegrador.*
import src.nivel1.*
import src.nivel2.*

program juego {
    // game.tittle()
    game.width(10)
    game.height(10)
    game.cellSize(102)
    game.boardGround("fondo3.png")

    nivel1.jugar()
}